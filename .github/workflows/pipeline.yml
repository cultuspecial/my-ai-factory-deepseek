name: 🏭 AI Production Factory (Frozen L4)

on:
  schedule:
    - cron: '0 2 * * *'  # 每天北京时间 10:00
  workflow_dispatch:      # 允许手动触发

permissions:
  contents: write

# 🔒 关键设置：禁止取消进行中的任务，确保审计记录完整
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 准备 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 安装依赖
        # 仅依赖 requests，减少维护成本
        run: pip install requests

      - name: 🤖 执行 AI 核心逻辑
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
        run: |
          python crawler.py
          python summarize.py

      - name: 💾 数据持久化 (工厂硬核版)
        if: always()
        run: |
          # 1. 即使出错也不要让 GitHub 报错
          set +e 

          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # 2. 【核心变化】直接把远程仓库同步过来，放弃本地挣扎
          # 这样就不会报 "unstaged changes" 的错误了
          git fetch origin main
          git reset --soft origin/main

          # 3. 重新添加这次运行生成的产物
          git add run_history.jsonl raw_data.json ai_research_report.md || true

          # 4. 检查是否有变化
          if git diff --staged --quiet; then
            echo "没有新内容，跳过提交。"
            exit 0
          fi

          # 5. 提交并尝试推送
          git commit -m "chore: factory output update [skip ci]"
          
          # 6. 尝试推送。如果失败（竞争输了），就打印一条警告而不是报错
          git push origin main || echo "⚠️ 冲突发生，本次数据未推送到云端，下次运行会自动合并。"

      
          # 4. 创建提交（此时基于最新远程）
          git commit -m "chore: factory output update [skip ci]"
      
          # 5. 推送
          git push origin main

      - name: 📈 生成看板
        if: always()
        # 逻辑已移出，一行命令搞定
        run: python scripts/dashboard.py

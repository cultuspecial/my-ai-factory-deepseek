name: 🏭 AI Production Factory (Frozen L4)

on:
  schedule:
    - cron: '0 2 * * *'  # 每天北京时间 10:00
  workflow_dispatch:      # 允许手动触发

permissions:
  contents: write

# 🔒 关键设置：禁止取消进行中的任务，确保审计记录完整
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 准备 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 安装依赖
        # 仅依赖 requests，减少维护成本
        run: pip install requests

      - name: 🤖 执行 AI 核心逻辑
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
        run: |
          python crawler.py
          python summarize.py

      - name: 💾 数据持久化 (Git Ops - L4 终极版)
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # 1. 【核心修正】永远先同步远程，不制造本地提交，确保底座最新
          git pull --rebase origin main || echo "Pull failed, potentially new repo, continuing..."

          # 2. 暂存所有产出文件
          git add run_history.jsonl raw_data.json ai_research_report.md || true

          # 3. 防抖检查：如果没有内容变化，直接优雅退出
          if git diff --staged --quiet; then
            echo "No changes to commit, skipping push."
            exit 0
          fi

          # 4. 此时才创建提交（基于最新远程状态）
          git commit -m "chore: factory output update [skip ci]"

          # 5. 推送（冲突概率降至物理极限）
          git push origin main


      - name: 📈 生成看板
        if: always()
        # 逻辑已移出，一行命令搞定
        run: python scripts/dashboard.py
